# analyze-measurement-py
analyzeMeasurement python version. Also contains the Foc48 transferred into python.

### Start up
```bash
python -m venv venv
venv\Scripts\Activate

pip install ttkbootstrap
pip install matplotlib
pip install numpy
pip install scipy
```
#### Run
just execute gui in the created enviroment, propper run_gui.py will be added